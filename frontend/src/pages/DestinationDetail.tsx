import { useState } from "react";
import { Link, useParams } from "react-router-dom";
import { ArrowDown, ArrowUpRight, Check, ChevronRight, MapPin, MessageCircle, Minus, Plus } from "lucide-react";
import { buttonVariants } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { getDestination } from "@/data/destinations";
import { openWhatsApp } from "@/lib/whatsapp";
import { cn } from "@/lib/utils";
import { Breadcrumbs, EnquiryForm, SectionHeading, Seo, SiteLayout } from "@/components/site/Shared";

export default function DestinationDetail() {
  const { slug } = useParams();
  const destination = getDestination(slug) ?? getDestination("bali")!;
  const [activeItinerary, setActiveItinerary] = useState(destination.itineraries[0]?.id);
  const [openDays, setOpenDays] = useState<string[]>([destination.itineraries[0]?.days[0]?.day ?? "01"]);
  const activeJourney = destination.itineraries.find((item) => item.id === activeItinerary) ?? destination.itineraries[0];
  const isBali = destination.slug === "bali";
  return <SiteLayout><Seo title={`${destination.name} journeys`} description={destination.heroDescription} />
    <section className="relative min-h-[650px] overflow-hidden bg-[#0d7a84] px-5 pb-16 pt-36 text-white sm:px-8 lg:px-12 lg:pt-44" data-testid="destination-hero"><img src={destination.image} alt={`${destination.name} landscape`} className="absolute inset-0 h-full w-full object-cover opacity-65" /><div className="absolute inset-0 bg-gradient-to-r from-[#0d353b]/90 via-[#0d353b]/40 to-transparent" /><div className="relative mx-auto max-w-[1440px]"><Breadcrumbs items={[{ label: "Destinations", href: "/destinations" }, { label: destination.name }]} /><div className="mt-24 max-w-3xl"><p className="font-mono text-xs uppercase tracking-[0.24em] text-[#a8f1ef]">{destination.country} · {destination.region}</p><h1 className="mt-4 font-serif text-6xl leading-[0.95] sm:text-8xl lg:text-[9rem]">{destination.name}</h1><p className="mt-6 max-w-xl text-lg leading-relaxed text-white/75">{destination.heroDescription}</p><div className="mt-8 flex flex-wrap gap-6 border-t border-white/25 pt-5 text-sm text-white/75"><span><strong className="block font-serif text-xl text-white">{destination.duration}</strong>Typical journey</span><span><strong className="block font-serif text-xl text-white">{destination.bestTime}</strong>Best time to visit</span><span><strong className="block font-serif text-xl text-white">{destination.popularWith}</strong>Popular with</span></div><div className="mt-8 flex flex-col gap-3 sm:flex-row"><a href="#request" className={cn(buttonVariants({ variant: "secondary", size: "lg" }), "justify-center gap-2 bg-white text-[#0d7a84] hover:bg-[#e5f7f5]")} data-testid="destination-request-button">Request this trip <ArrowDown className="h-4 w-4" /></a><button type="button" onClick={() => openWhatsApp(`Hi Tentwood Trips! I’m interested in a ${destination.name} journey.`)} className="inline-flex h-11 items-center justify-center gap-2 border border-white/40 px-5 text-sm font-medium text-white transition-colors hover:bg-white/10" data-testid="destination-whatsapp-button">WhatsApp Tentwood <MessageCircle className="h-4 w-4" /></button></div></div></div></section>
    <section className="mx-auto max-w-[1440px] px-5 py-16 sm:px-8 lg:px-12 lg:py-24"><div className="grid gap-12 lg:grid-cols-[0.85fr_1.15fr] lg:items-center"><div><SectionHeading eyebrow="A closer look" title={`About ${destination.name}`} copy={destination.about} /><div className="mt-7 flex flex-wrap gap-2">{destination.tags.map((tag) => <Badge key={tag} variant="outline" className="rounded-none border-[#0d7a84]/25 px-3 py-1 font-mono text-[9px] uppercase tracking-[0.16em] text-[#0d7a84]">{tag}</Badge>)}</div></div><div className="grid grid-cols-2 gap-3"><img src={destination.secondaryImage} alt={`${destination.name} architecture or landscape`} className="aspect-[4/5] w-full object-cover" loading="lazy" /><img src={destination.image} alt={`${destination.name} travel moment`} className="mt-10 aspect-[4/5] w-full object-cover" loading="lazy" /></div></div></section>
    <section className="bg-[#f2ede4] px-5 py-16 sm:px-8 lg:px-12 lg:py-24"><div className="mx-auto max-w-[1440px]"><SectionHeading eyebrow="The reasons to go" title={`Why ${destination.name}?`} copy="The places that stay with you are usually the ones that offer more than one kind of beauty." /><div className="mt-12 grid gap-8 sm:grid-cols-2 lg:grid-cols-4">{(destination.reasons.length ? destination.reasons : [{ title: "A different pace", copy: "A considered route built around the details that make a place feel alive." }, { title: "The local table", copy: "Good food, found in the right rooms and made by the right hands." }, { title: "Room to wander", copy: "Enough structure to feel held, enough freedom to make it yours." }, { title: "Human guidance", copy: "An expert who knows when to recommend and when to leave you alone." }]).map((reason, index) => <div key={reason.title} className="border-t border-[#1a1d20]/15 pt-5"><span className="font-mono text-xs text-[#0d7a84]">0{index + 1}</span><h3 className="mt-8 font-serif text-2xl">{reason.title}</h3><p className="mt-3 text-sm leading-relaxed text-[#5a6065]">{reason.copy}</p></div>)}</div></div></section>
    {destination.experiences.length > 0 && <section className="mx-auto max-w-[1440px] px-5 py-16 sm:px-8 lg:px-12 lg:py-24"><SectionHeading eyebrow="Do it differently" title="Experiences worth making time for" copy="Not a checklist. Just a few possibilities to make the journey yours." /><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{destination.experiences.map((experience) => <article key={experience.title} className="group" data-testid={`experience-card-${experience.title.toLowerCase().replaceAll(" ", "-")}`}><div className="overflow-hidden"><img src={experience.image} alt={experience.title} loading="lazy" className="aspect-[4/5] w-full object-cover transition duration-700 group-hover:scale-105" /></div><div className="mt-4 flex items-start justify-between gap-4"><div><h3 className="font-serif text-2xl">{experience.title}</h3><p className="mt-2 text-sm leading-relaxed text-[#5a6065]">{experience.description}</p></div>{experience.duration && <span className="shrink-0 pt-1 font-mono text-[9px] uppercase tracking-[0.14em] text-[#0d7a84]">{experience.duration}</span>}</div></article>)}</div></section>}
    {isBali && <section className="bg-[#e9f3f1] px-5 py-16 sm:px-8 lg:px-12 lg:py-24" data-testid="itineraries-section"><div className="mx-auto max-w-[1440px]"><SectionHeading eyebrow="The main event" title="Choose your journey" copy="Each route is a starting point, designed to be made personal with our travel team." /><div className="mt-10 grid gap-8 lg:grid-cols-[0.85fr_1.15fr]"><div className="flex flex-col gap-3">{destination.itineraries.map((itinerary) => <button key={itinerary.id} type="button" onClick={() => { setActiveItinerary(itinerary.id); setOpenDays([itinerary.days[0].day]); }} className={cn("border p-5 text-left transition-colors", activeItinerary === itinerary.id ? "border-[#0d7a84] bg-[#0d7a84] text-white" : "border-[#1a1d20]/10 bg-[#fdfbf7] hover:border-[#0d7a84]/40")} data-testid={`itinerary-select-${itinerary.id}`}><div className="flex items-start justify-between gap-4"><div><span className={cn("font-mono text-[10px] uppercase tracking-[0.18em]", activeItinerary === itinerary.id ? "text-[#a8f1ef]" : "text-[#0d7a84]")}>{itinerary.duration}</span><h3 className="mt-2 font-serif text-2xl">{itinerary.name}</h3><p className={cn("mt-1 text-xs", activeItinerary === itinerary.id ? "text-white/65" : "text-[#5a6065]")}>{itinerary.route}</p></div><ChevronRight className="h-5 w-5" /></div><div className="mt-5 flex items-center justify-between border-t border-current/15 pt-4"><span className="text-xs opacity-75">Starting from</span><span className="font-serif text-lg">{itinerary.price}</span></div></button>)}</div>{activeJourney && <div className="border border-[#1a1d20]/10 bg-[#fdfbf7] p-5 sm:p-8"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div><span className="font-mono text-[10px] uppercase tracking-[0.18em] text-[#0d7a84]">{activeJourney.route}</span><h3 className="mt-2 font-serif text-3xl">{activeJourney.name}</h3><p className="mt-2 max-w-lg text-sm leading-relaxed text-[#5a6065]">{activeJourney.summary}</p></div><Link to="#request" className="inline-flex items-center gap-1 text-sm text-[#0d7a84]" data-testid={`itinerary-request-${activeJourney.id}`}>Request this trip <ArrowUpRight className="h-4 w-4" /></Link></div><div className="mt-8 border-t border-[#1a1d20]/10 pt-5"><p className="font-mono text-[10px] uppercase tracking-[0.18em] text-[#0d7a84]">A day-by-day sketch</p><div className="mt-4">{activeJourney.days.map((day) => { const open = openDays.includes(day.day); return <div key={day.day} className="border-b border-[#1a1d20]/10" data-testid={`itinerary-day-${day.day}`}><button type="button" onClick={() => setOpenDays((current) => open ? current.filter((item) => item !== day.day) : [...current, day.day])} className="flex w-full items-center gap-4 py-4 text-left" data-testid={`itinerary-day-toggle-${day.day}`}><span className="font-mono text-xs text-[#0d7a84]">{day.day}</span><span className="flex-1 font-serif text-lg">{day.title}</span>{open ? <Minus className="h-4 w-4 text-[#0d7a84]" /> : <Plus className="h-4 w-4 text-[#0d7a84]" />}</button>{open && <div className="grid gap-4 pb-5 pl-10 sm:grid-cols-[1fr_auto]"><p className="text-sm leading-relaxed text-[#5a6065]">{day.details}</p><div className="flex flex-wrap gap-2 sm:justify-end">{day.tags.map((tag) => <span key={tag} className="h-fit bg-[#e9f3f1] px-2 py-1 font-mono text-[9px] uppercase tracking-[0.12em] text-[#0d7a84]">{tag}</span>)}</div></div>}</div>; })}</div></div></div>}</div></div></section>}
    <section className="mx-auto max-w-[1440px] px-5 py-16 sm:px-8 lg:px-12 lg:py-24"><div className="grid gap-6 md:grid-cols-2"><div className="border border-[#0d7a84]/15 bg-[#e9f3f1] p-6 sm:p-8"><h3 className="font-mono text-[10px] uppercase tracking-[0.2em] text-[#0d7a84]">Inclusions</h3><ul className="mt-5 space-y-3 text-sm text-[#1a1d20]">{(activeJourney?.inclusions ?? ["Handpicked accommodation", "Selected experiences", "Local assistance", "Daily breakfast"]).map((item) => <li key={item} className="flex gap-3"><Check className="h-4 w-4 shrink-0 text-[#0d7a84]" />{item}</li>)}</ul></div><div className="border border-[#1a1d20]/10 bg-[#f8f5ee] p-6 sm:p-8"><h3 className="font-mono text-[10px] uppercase tracking-[0.2em] text-[#5a6065]">Exclusions</h3><ul className="mt-5 space-y-3 text-sm text-[#5a6065]">{(activeJourney?.exclusions ?? ["International flights", "Personal expenses", "Meals not mentioned", "Travel insurance unless specified"]).map((item) => <li key={item} className="flex gap-3"><span className="text-[#d98a6c]">×</span>{item}</li>)}</ul></div></div></section>
    <section className="bg-[#f2ede4] px-5 py-16 sm:px-8 lg:px-12 lg:py-24"><div className="mx-auto max-w-[1440px]"><div className="grid gap-8 lg:grid-cols-[0.7fr_1.3fr] lg:items-end"><SectionHeading eyebrow="From people who went" title="The details make the memory." /><div className="grid gap-4 sm:grid-cols-2">{destination.reviews.map((review) => <blockquote key={review.name} className="border-l-2 border-[#0d7a84] pl-5"><p className="font-serif text-xl leading-snug">“{review.quote}”</p><footer className="mt-4 text-xs text-[#5a6065]">{review.name} <span className="mx-1">·</span> {review.trip}</footer></blockquote>)}</div></div></div></section>
    <section id="request" className="mx-auto max-w-[1440px] scroll-mt-20 px-5 py-16 sm:px-8 lg:px-12 lg:py-24"><div className="grid gap-10 lg:grid-cols-[0.7fr_1.3fr] lg:items-start"><div className="lg:sticky lg:top-28"><SectionHeading eyebrow="Make it yours" title="Ready when you are." copy="Start with one detail or bring us the whole dream. There’s no wrong way to begin." /><div className="mt-8 flex items-center gap-3 border-t border-[#1a1d20]/10 pt-5"><span className="grid h-10 w-10 place-items-center rounded-full bg-[#e9f3f1] text-[#0d7a84]"><MapPin className="h-4 w-4" /></span><div><p className="text-sm font-medium">{destination.name}, your way</p><p className="text-xs text-[#5a6065]">From {destination.priceFrom} per person</p></div></div></div><EnquiryForm destination={destination} itinerary={activeJourney?.name} /></div></section>
    <section className="bg-[#0d7a84] px-5 py-14 text-center text-white sm:px-8 lg:px-12" data-testid="destination-final-cta"><p className="font-mono text-[10px] uppercase tracking-[0.2em] text-[#a8f1ef]">Still deciding?</p><h2 className="mt-3 font-serif text-4xl">Talk to a Tentwood travel expert.</h2><button type="button" onClick={() => openWhatsApp(`Hi Tentwood Trips! I’m still deciding about ${destination.name}.`)} className="mt-7 inline-flex h-11 items-center gap-2 border border-white/40 px-5 text-sm transition-colors hover:bg-white/10" data-testid="destination-final-whatsapp-button">WhatsApp Tentwood <MessageCircle className="h-4 w-4" /></button></section>
  </SiteLayout>;
}